/**
 * Historical stub — migration already applied on existing DBs.
 * Schema was removed by 082_drop_payments_module.js.
 * Kept on disk so Knex migration history stays valid.
 */
export async function up() {}
export async function down() {}
