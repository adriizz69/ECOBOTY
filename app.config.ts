export default defineAppConfig({
  ui: {
    button: {
      default: {
        size: "sm",
        variant: "solid",
        color: "neutral"
      }
    },
    card: {
      base: "rounded-2xl shadow-sm"
    }
  }
});
